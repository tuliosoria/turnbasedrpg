# Backup dos verbetes

Cópia integral dos itens `WIKI#` antes de qualquer reescrita automática.
Um arquivo por execução, nomeado pela data.

Para restaurar um verbete, veja `backend/scripts/restore-wiki.mjs`.
